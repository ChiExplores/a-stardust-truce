const Node = (val, next) => {
  let node = {}
  node.val = val, node.next = next;
  return node;
}
const pojaposdjpaojspdojasd = _ => {
  let obj = {}
// list-push
  obj.push = node => {
    node = Node(node, obj.head);
    obj.head = node;
    if (obj.hasOwnProperty('size')) obj.size++;
  };
// list-pop
  obj.pop = _ => {
    let pop = obj.head || null;
    obj.head = pop ? pop.next : null;
    if (obj.hasOwnProperty('size')) obj.size--;
    return pop ? pop.val : undefined;
  }
  return obj
}